# iy26m9d5h15
## 版本情况
|版本号码|版本代号|版本编号|开发日期|完成日期|
|---|---|---|---|---|
|`iy26m9d5h15`|**inv-1**|0|_2026-09-5_|-|
### 编译:
` g++ -std=c++17 src/main.cpp src/preprocessor.cpp src/utils.cpp -o vl `
## 更新内容

### VexLang开发手册

#### 引言
这个版本只是一个框架，所以先写开发手册

#### 注释
```vl
// 单行注释
/*
多行注释
*/
```

#### 类型
```vl
// 整数
int8, int16, int32, int64
unt8, unt16, unt32, unt64

// 浮点数
float32, float64

// 其他
bool
void
auto
file

// 指针
T*

// 字面量
true, false
null, nullptr, 0
```

#### 变量
```vl
int8 a;
int8 b = 0;
auto c = 10;           // 自动推断
const int8 d = 100;    // 只读
```

#### 字符串
```vl
// 没有内置字符串类型，用字符指针
int8* str = "hello";
```

#### 类型转换
```vl
int8(值)    // 函数风格
```

#### 运算符
```vl
// 算术
+  -  *  /  %  ++  --

// 比较
==  !=  >  <  >=  <=

// 逻辑
&&  ||  !

// 赋值
=  +=  -=  *=  /=  %=
```

#### 控制流
```vl
if (条件) {
    // 代码
} else {
    // 代码
}

while (条件) {
    // 代码
}

do {
    // 代码
} while (条件);

for (i = 0; i < 10; i++) {
    // 代码
}

for (item in 数组) {
    // 代码
}

switch (变量) {
    case 值1:
        // 代码
        break;
    case 值2:
        // 代码
        break;
    default:
        // 代码
}

break;
continue;
return 值;
```

#### 函数
```vl
fn 函数名(参数列表) {
    // 代码
}

fn 函数名(参数列表) -> 返回类型 {
    return 值;
}

// 调用
函数名(实参列表);
```

#### 类
```vl
// 和C++一样
class 类名 {
public:
    成员变量;
    成员函数();
private:
    成员变量;
    成员函数();
protected:
    成员变量;
    成员函数();
};

// 继承
class 子类 : public 父类 {
};

// 构造函数/析构函数
class 类名 {
    类名(参数) {}
    ~类名() {}
};

// 虚函数
virtual fn 函数名() {}

// 纯虚函数
virtual fn 函数名() = 0;
```

#### 模板
```vl
template <typename T>
class 类名 {
    T 变量;
};

template <typename T>
fn 函数名(T 参数) {}
```

#### 异常
```vl
try {
    throw 异常;
} catch (类型 变量) {
    // 处理
}
```

#### 预处理器
```vl
#inc "file.vl"      // 项目路径，自动防重复包含
#inc <file.vl>      // 系统路径，自动防重复包含

#macro 宏名(参数) {
    // 纯文本替换
}

#macro 宏名 值      // 常量宏
```

#### 系统IO
```vl
// 内置类型
file

// 内置常量
stdin    // file类型
stdout   // file类型
stderr   // file类型

// 关键字
open(路径, 标志) -> file
read(文件描述符, 缓冲区, 大小) -> 读取字节数，-1表示错误
write(文件描述符, 缓冲区, 大小) -> 写入字节数，-1表示错误
close(文件描述符)

// open标志常量
O_RDONLY
O_WRONLY
O_RDWR
O_CREAT
O_TRUNC
O_APPEND
// ... 其他和C++一样

// 示例
file f = open("test.txt", O_RDONLY);
int8 buf[256];
int32 n = read(f, buf, 256);
write(stdout, buf, n);
close(f);
```

#### 其他
```vl
// 和C++一样的内容
// 关键字、运算符重载、多重继承、命名空间等全部和C++相同
```